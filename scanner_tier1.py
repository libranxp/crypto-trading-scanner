def run_tier1_scan():
    """
    Tier 1: Basic filters on price, volume, market cap, RSI, RVOL
    Replace placeholders with live API calls (CMC, Messari, etc.)
    """
    candidates = []

    # Example placeholder: fetch top 200 from CMC
    # Apply filters: $0.005 – $50, volume > $15M, mcap $20M–$2B, RSI 50–70, RVOL > 2
    # TODO: integrate actual API fetch

    return candidates
